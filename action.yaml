name: "Summarize commits & send client email"
description: "Summarizes recent commit messages with OpenAI and e sends an email"
author: "Serbyte (Austin Serb)"
branding:
  icon: "mail"
  color: "blue"

inputs:
  openai-model:
    description: "OpenAI chat model ID (e.g., gpt-4o-mini)"
    required: false
    default: "gpt-4o-mini"
  site-label:
    description: "Site label to use in the e-mail subject and template"
    required: false
    default: ""

outputs:
  email_body:
    description: "HTML-formatted summary (ready for sending)"
  summary_md:
    description: "Markdown summary (for logs/PRs)"

runs:
  using: "node20"
  main: "dist/index.js"
